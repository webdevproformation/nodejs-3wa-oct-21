const { Router } = require("express")

const router = Router()


router.post("/creer-produit" , async ( req, rep ) => {})


module.exports = router;

// 1 collection (table) + model dédié (CRUD + opération sur cette table) + route dédié 